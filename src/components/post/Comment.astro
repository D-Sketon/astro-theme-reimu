---
import { VALINE, WALINE, GITALK, GISCUS, UTTERANCES, TWIKOO, DISQUS } from "../../utils/config";

import ValineComment from "./ValineComment.astro";
import WalineComment from "./WalineComment.astro";
import GitalkComment from "./GitalkComment.astro";
import GiscusComment from "./GiscusComment.astro";
import UtterancesComment from "./UtterancesComment.astro";
import TwikooComment from "./TwikooComment.astro";
import DisqusComment from "./DisqusComment.astro";

type Props = {
  comment: boolean;
};

const { comment } = Astro.props;
---

{comment && VALINE.enable && <ValineComment />}
{comment && WALINE.enable && <WalineComment />}
{comment && GITALK.enable && <GitalkComment />}
{comment && GISCUS.enable && <GiscusComment />}
{comment && TWIKOO.enable && <TwikooComment />}
{comment && DISQUS.enable && <DisqusComment />}
{comment && UTTERANCES.enable && <UtterancesComment />}
<style lang="stylus" is:global>
  #comments {
    background: var(--color-wrap);
    padding: 20px 36px;
    border-radius: 12px;
    margin: 12px 0;

    @media screen and (max-width: 767px) {
      padding: 20px 16px;
    }

    a {
      color: var(--color-link);
    }
  }
</style>
